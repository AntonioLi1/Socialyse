import database from '@react-native-firebase/database';
import auth from '@react-native-firebase/auth';


async function DisplayLocationPins(token, userLongitude, userLatitude) {
    auth().onAuthStateChanged
}

async function ViewChannelsInPin(token, pinLongitude, pinLatitude) {

}

// ????
function MakeLocationPin(token, ) {

}

export { DisplayLocationPins, ViewChannelsInPin, MakeLocationPin }